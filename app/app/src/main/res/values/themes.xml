<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies the real theme; this only needs to be a Material 3
         base so the splash and system bars do not flash a stock blue. -->
    <style name="Theme.NseAnalysis" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/surface</item>
    </style>
</resources>
