<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. Lets the app read a scan feed served from the developer
  machine over plain HTTP: 10.0.2.2 is the emulator's alias for the host's
  loopback interface. Release builds never see this file, so cleartext stays
  blocked everywhere it matters.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
